<Window x:Class="MqPrinterFixer.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MqPrinterFixer.App.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="{Binding Title}"
        Height="780"
        Width="1280"
        MinHeight="700"
        MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource HeaderBackgroundBrush}"
                BorderBrush="{DynamicResource DividerBrush}"
                BorderThickness="0,0,0,1"
                Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="MQ Printer Fixer"
                                   Style="{StaticResource H3TextStyle}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text=" — "
                                   Style="{StaticResource CaptionTextStyle}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Subtitle}"
                                   Style="{StaticResource SmallTextStyle}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="{Binding ComputerName}" Style="{StaticResource SmallTextStyle}"/>
                        <TextBlock Text=" · " Style="{StaticResource CaptionTextStyle}"/>
                        <TextBlock Text="{Binding PrinterRoleDisplay}"
                                   Style="{StaticResource SmallTextStyle}"
                                   FontWeight="SemiBold"/>
                        <TextBlock Text=" · " Style="{StaticResource CaptionTextStyle}"/>
                        <TextBlock Text="{Binding NetworkProfileDisplay}" Style="{StaticResource SmallTextStyle}"/>
                        <TextBlock Text=" · " Style="{StaticResource CaptionTextStyle}"/>
                        <TextBlock Text="{Binding WindowsEditionDisplay}" Style="{StaticResource SmallTextStyle}"/>
                        <TextBlock Text=" · " Style="{StaticResource CaptionTextStyle}"/>
                        <TextBlock Text="{Binding AdministratorDisplay, StringFormat='Admin: {0}'}"
                                   Style="{StaticResource SmallTextStyle}"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentThemeDisplay, StringFormat='Theme: {0}'}"
                               Style="{StaticResource SmallTextStyle}"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0"/>

                    <Button Content="Rename Computer"
                            Click="OnRenameComputerClick"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,8,0"/>

                    <Button Content="Toggle Theme"
                            Command="{Binding CycleThemeCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{DynamicResource SidebarBackgroundBrush}"
                    BorderBrush="{DynamicResource DividerBrush}"
                    BorderThickness="0,0,1,0">
                <ListBox ItemsSource="{Binding NavigationItems}"
                         SelectedItem="{Binding SelectedNavigationItem, Mode=TwoWay}"
                         Style="{StaticResource SidebarListBoxStyle}"
                         ItemContainerStyle="{StaticResource SidebarItemStyle}"
                         ItemTemplate="{StaticResource SidebarItemTemplate}"/>
            </Border>

            <ContentControl Grid.Column="1" Content="{Binding CurrentPage}"/>
        </Grid>

        <Border Grid.Row="2"
                Background="{DynamicResource ConsoleBackgroundBrush}"
                BorderBrush="{DynamicResource DividerBrush}"
                BorderThickness="0,1,0,0">
            <TextBlock Text="Console — sẽ hoàn thiện ở Phase 21"
                       Foreground="#8A8A8A"
                       Style="{StaticResource MonospaceTextStyle}"
                       Margin="16"
                       VerticalAlignment="Top"/>
        </Border>
    </Grid>
</Window>