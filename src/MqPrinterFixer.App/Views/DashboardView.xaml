<UserControl x:Class="MqPrinterFixer.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MqPrinterFixer.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:DashboardViewModel}">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  Background="{DynamicResource AppBackgroundBrush}">
        <StackPanel Margin="24">

            <!-- Header row: title + refresh -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding Title}" Style="{StaticResource H1TextStyle}"/>
                    <TextBlock Text="{Binding Description}"
                               Style="{StaticResource SecondaryTextStyle}"
                               Margin="0,6,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Refresh"
                        Command="{Binding LoadCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        VerticalAlignment="Top"/>
            </Grid>

            <!-- 3 info cards -->
            <UniformGrid Columns="3" Margin="0,24,0,0">

                <Border Style="{StaticResource CardStyle}" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Computer" Style="{StaticResource SectionHeaderStyle}"/>
                        <TextBlock Text="{Binding ComputerInfo.ComputerName, TargetNullValue='Loading...'}"
                                   Style="{StaticResource H2TextStyle}"/>
                        <TextBlock Text="{Binding ComputerInfo.UserName, StringFormat='User: {0}', TargetNullValue=''}"
                                   Style="{StaticResource SmallTextStyle}"
                                   Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}" Margin="8,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Windows" Style="{StaticResource SectionHeaderStyle}"/>
                        <TextBlock Text="{Binding ComputerInfo.EditionDisplayName, TargetNullValue='Loading...'}"
                                   Style="{StaticResource H3TextStyle}"/>
                        <TextBlock Text="{Binding ComputerInfo.BuildNumber, StringFormat='Build: {0}', TargetNullValue=''}"
                                   Style="{StaticResource SmallTextStyle}"
                                   Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Privileges" Style="{StaticResource SectionHeaderStyle}"/>
                        <TextBlock Text="{Binding ComputerInfo.IsAdministrator, StringFormat='Administrator: {0}', TargetNullValue='Loading...'}"
                                   Style="{StaticResource H3TextStyle}"/>
                        <TextBlock Text="Read-only access. Elevation chỉ cần cho thao tác thay đổi."
                                   Style="{StaticResource SmallTextStyle}"
                                   Margin="0,6,0,0"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

            </UniformGrid>

            <!-- Loading indicator: CHỈ dùng TextBlock.Style element, KHÔNG có Style attribute -->
            <TextBlock Text="Loading system information..."
                       HorizontalAlignment="Center"
                       Margin="0,24,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource CaptionTextStyle}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

        </StackPanel>
    </ScrollViewer>
</UserControl>