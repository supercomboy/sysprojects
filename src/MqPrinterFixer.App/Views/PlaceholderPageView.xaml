<UserControl x:Class="MqPrinterFixer.App.Views.PlaceholderPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MqPrinterFixer.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PageViewModelBase}">

    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                    MaxWidth="720">

            <TextBlock Text="{Binding Title}"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       FontSize="{DynamicResource FontSizeH1}"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"/>

            <TextBlock Text="{Binding Description}"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="{DynamicResource FontSizeBody}"
                       Margin="0,14,0,0"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"/>

            <Border Background="{DynamicResource CardBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="{DynamicResource CardCornerRadius}"
                    Padding="{DynamicResource CardPadding}"
                    Margin="0,32,0,0">
                <TextBlock Text="Content cho page này sẽ được triển khai ở các Phase sau."
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontSize="{DynamicResource FontSizeSmall}"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"/>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>