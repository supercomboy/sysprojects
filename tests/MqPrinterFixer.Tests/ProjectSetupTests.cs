namespace MqPrinterFixer.Tests;

public class ProjectSetupTests
{
    [Fact]
    public void TestProject_IsWiredCorrectly()
    {
        // Sanity test chỉ để xác nhận test project build và chạy được.
        // Sẽ bị xóa hoặc thay thế khi bắt đầu Phase 26 (Unit Tests thật).
        Assert.True(true);
    }
}